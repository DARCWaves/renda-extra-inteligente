
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: "Inter", sans-serif;
  color: #fff;
  background:
    radial-gradient(circle at 20% 0%, rgba(212,175,55,0.15), transparent),
    radial-gradient(circle at 80% 100%, rgba(255,255,255,0.05), transparent),
    #050507;
}

/* HEADER */

.header-saas {
  position: sticky;
  top: 0;
  z-index: 1000;
  padding: 16px 24px;
  display: flex;
  justify-content: space-between;
  align-items: center;

  background: rgba(10,10,15,0.7);
  backdrop-filter: blur(16px);
  border-bottom: 1px solid rgba(255,255,255,0.05);
}

/* LOGO */

.logo-area {
  display: flex;
  align-items: center;
  gap: 10px;
}

.logo-area img {
  height: 38px;
}

.logo-text strong {
  display: block;
  font-size: 14px;
  background: linear-gradient(90deg,#fff,#d4af37);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}

.logo-text span {
  font-size: 11px;
  color: #aaa;
}

/* NAV */

.nav-saas {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
}

.nav-saas a {
  padding: 8px 12px;
  border-radius: 999px;
  text-decoration: none;
  font-size: 13px;
  color: #aaa;
  transition: 0.2s;
}

.nav-saas a:hover {
  background: rgba(255,255,255,0.08);
  color: #fff;
}

/* CTA NAV */

.cta-nav {
  background: linear-gradient(90deg,#d4af37,#fff);
  color: #000 !important;
  font-weight: bold;
}

/* MAIN */

main {
  max-width: 1100px;
  margin: auto;
  padding: 32px 16px;
}

/* SECTIONS */

section {
  margin-bottom: 28px;
  padding: 22px;
  border-radius: 20px;

  background: rgba(255,255,255,0.03);
  border: 1px solid rgba(255,255,255,0.05);

  backdrop-filter: blur(8px);
}

/* HERO */

.hero-home {
  padding: 32px;
  background:
    linear-gradient(135deg, rgba(212,175,55,0.15), rgba(255,255,255,0.03)),
    rgba(255,255,255,0.02);

  border: 1px solid rgba(212,175,55,0.2);
}

.hero-home h2 {
  font-size: 36px;
  margin-bottom: 12px;
}

.hero-home p {
  color: #ccc;
  max-width: 600px;
}

/* BOTÕES */

.btn-primary {
  display: inline-block;
  margin-top: 12px;
  padding: 10px 18px;
  border-radius: 999px;

  background: linear-gradient(90deg,#d4af37,#fff);
  color: #000;
  font-weight: bold;
  text-decoration: none;
}

.btn-primary:hover {
  transform: translateY(-1px);
}

/* POSTS */

.post-preview {
  padding: 18px;
  border-radius: 16px;

  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.05);

  transition: 0.2s;
}

.post-preview:hover {
  border-color: rgba(212,175,55,0.4);
}

.post-preview h4 {
  margin-bottom: 6px;
}

/* GRID */

.indicator-grid {
  display: grid;
  gap: 14px;
  grid-template-columns: repeat(auto-fit, minmax(220px,1fr));
}

/* CARDS */

.indicator-card {
  padding: 16px;
  border-radius: 16px;

  background: rgba(255,255,255,0.04);
  border: 1px solid rgba(255,255,255,0.05);
}

.indicator-card strong {
  display: block;
  margin: 10px 0;
  font-size: 20px;
  color: #d4af37;
}

/* FOOTER */

.footer-saas {
  text-align: center;
  padding: 40px 0;
  color: #777;
}

